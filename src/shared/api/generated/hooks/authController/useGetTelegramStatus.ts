/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetTelegramStatusQueryResponse,
  GetTelegramStatusPathParams,
} from "../../types/GetTelegramStatus";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getTelegramStatus } from "../../clients/authController/getTelegramStatus";

export const getTelegramStatusQueryKey = (
  challengeId: GetTelegramStatusPathParams["challengeId"],
) =>
  [
    {
      url: "/api/auth/telegram/status/:challengeId",
      params: { challengeId: challengeId },
    },
  ] as const;

export type GetTelegramStatusQueryKey = ReturnType<
  typeof getTelegramStatusQueryKey
>;

export function getTelegramStatusQueryOptions(
  challengeId: GetTelegramStatusPathParams["challengeId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getTelegramStatusQueryKey(challengeId);
  return queryOptions<
    GetTelegramStatusQueryResponse,
    ResponseErrorConfig<Error>,
    GetTelegramStatusQueryResponse,
    typeof queryKey
  >({
    enabled: !!challengeId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getTelegramStatus(challengeId, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * {@link /api/auth/telegram/status/:challengeId}
 */
export function useGetTelegramStatus<
  TData = GetTelegramStatusQueryResponse,
  TQueryData = GetTelegramStatusQueryResponse,
  TQueryKey extends QueryKey = GetTelegramStatusQueryKey,
>(
  challengeId: GetTelegramStatusPathParams["challengeId"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetTelegramStatusQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getTelegramStatusQueryKey(challengeId);

  const query = useQuery(
    {
      ...getTelegramStatusQueryOptions(challengeId, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
