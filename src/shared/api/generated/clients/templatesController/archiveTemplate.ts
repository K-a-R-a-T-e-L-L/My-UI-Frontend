/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../../client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type {
  ArchiveTemplateMutationResponse,
  ArchiveTemplatePathParams,
} from "../../types/ArchiveTemplate";

function getArchiveTemplateUrl(
  templateId: ArchiveTemplatePathParams["templateId"],
) {
  const res = {
    method: "POST",
    url: `/api/templates/${templateId}/archive` as const,
  };
  return res;
}

/**
 * {@link /api/templates/:templateId/archive}
 */
export async function archiveTemplate(
  templateId: ArchiveTemplatePathParams["templateId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ArchiveTemplateMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getArchiveTemplateUrl(templateId).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
