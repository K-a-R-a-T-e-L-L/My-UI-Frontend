/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../../client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type {
  HandleTelegramWebhookMutationResponse,
  HandleTelegramWebhookHeaderParams,
} from "../../types/HandleTelegramWebhook";

function getHandleTelegramWebhookUrl() {
  const res = { method: "POST", url: `/api/auth/telegram/webhook` as const };
  return res;
}

/**
 * {@link /api/auth/telegram/webhook}
 */
export async function handleTelegramWebhook(
  headers: HandleTelegramWebhookHeaderParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    HandleTelegramWebhookMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getHandleTelegramWebhookUrl().url.toString(),
    ...requestConfig,
    headers: { ...headers, ...requestConfig.headers },
  });
  return res.data;
}
