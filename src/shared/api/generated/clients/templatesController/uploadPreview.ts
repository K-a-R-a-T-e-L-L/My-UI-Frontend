/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../../client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type { UploadPreviewMutationResponse } from "../../types/UploadPreview";

function getUploadPreviewUrl() {
  const res = { method: "POST", url: `/api/templates/preview/upload` as const };
  return res;
}

/**
 * {@link /api/templates/preview/upload}
 */
export async function uploadPreview(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    UploadPreviewMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getUploadPreviewUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
