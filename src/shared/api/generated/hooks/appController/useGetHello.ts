/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type { GetHelloQueryResponse } from "../../types/GetHello";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getHello } from "../../clients/appController/getHello";

export const getHelloQueryKey = () => [{ url: "/api" }] as const;

export type GetHelloQueryKey = ReturnType<typeof getHelloQueryKey>;

export function getHelloQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getHelloQueryKey();
  return queryOptions<
    GetHelloQueryResponse,
    ResponseErrorConfig<Error>,
    GetHelloQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getHello({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * {@link /api}
 */
export function useGetHello<
  TData = GetHelloQueryResponse,
  TQueryData = GetHelloQueryResponse,
  TQueryKey extends QueryKey = GetHelloQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetHelloQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getHelloQueryKey();

  const query = useQuery(
    {
      ...getHelloQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
