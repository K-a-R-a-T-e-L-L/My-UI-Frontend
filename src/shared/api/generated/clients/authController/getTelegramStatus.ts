/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../../client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type {
  GetTelegramStatusQueryResponse,
  GetTelegramStatusPathParams,
} from "../../types/GetTelegramStatus";

function getGetTelegramStatusUrl(
  challengeId: GetTelegramStatusPathParams["challengeId"],
) {
  const res = {
    method: "GET",
    url: `/api/auth/telegram/status/${challengeId}` as const,
  };
  return res;
}

/**
 * {@link /api/auth/telegram/status/:challengeId}
 */
export async function getTelegramStatus(
  challengeId: GetTelegramStatusPathParams["challengeId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTelegramStatusQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetTelegramStatusUrl(challengeId).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
