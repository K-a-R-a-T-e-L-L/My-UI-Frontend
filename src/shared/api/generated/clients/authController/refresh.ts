/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../../client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "../../../client";
import type { RefreshMutationResponse } from "../../types/Refresh";

function getRefreshUrl() {
  const res = { method: "POST", url: `/api/auth/refresh` as const };
  return res;
}

/**
 * {@link /api/auth/refresh}
 */
export async function refresh(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    RefreshMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "POST", url: getRefreshUrl().url.toString(), ...requestConfig });
  return res.data;
}
